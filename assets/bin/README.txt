Place a compatible signed/verified wg-quick (and its required WireGuard binaries) here if you later decide to ship the WireGuard userspace tools inside the application.

Do not download arbitrary binaries into this directory. Pin a known upstream release, verify checksums, and code-sign the final application.
